# 贡献指南

感谢你有兴趣参与 LocalVault。这是一个**处理用户最敏感数据**的项目，因此对贡献的要求比一般项目更严一些。

---

## ⚠️ 第一条：贡献前必须同意 CLA

提交任何 Pull Request 之前，请先阅读并同意 **[`CLA.md`](CLA.md)**。

### 为什么必须这么做

LocalVault 以 MIT 许可证开源。但如果没有 CLA，**项目会永久失去变更许可证的能力** —— 因为每个贡献者对自己写的代码自动拥有版权，变更项目整体许可证需要所有版权人的同意。一旦合并了十个人的 PR，想改证就要找这十个人逐一同意。

这份 CLA 解决该问题，同时保证：

- 你**保留**自己贡献的版权（是**许可授予**，不是版权转移）
- 你仍可把自己的贡献用于任何其他目的，包括商业用途
- 授予项目的许可是**非独占**的
- 附带**专利许可**，且含防御性终止条款

### 签署方式

首次提交 PR 时，CLA 助手机器人会留言，按指示回复即可。**只需签署一次**，此后所有 PR 自动覆盖。

**如果不接受 CLA 中的再许可条款**，请不要提交贡献。你仍然可以自由使用本项目（MIT 赋予你全部使用权利），也可以 fork 后自行维护。我们尊重这个选择。

> **常见误解澄清**：DCO（`Signed-off-by`）**不能替代 CLA**。DCO 是一份"证明"而非合同，它只证明你有权按**项目当前的许可证**提交代码，**不授予再许可权**，也不含专利授权。本项目采用 CLA 而非 DCO，理由详见 `docs/04-系统设计.md` §12.3。

---

## 开发环境

| 要求 | 说明 |
| --- | --- |
| Rust | **>= 1.85**（由 `getrandom` 0.4.3 的 MSRV 决定） |
| 工具链 | stable channel（见 `core/rust-toolchain.toml`） |
| Python | 3.10+（仅用于 `tools/` 下的辅助脚本，非构建依赖） |

```bash
git clone <repo-url>
cd mypassword/core
cargo build
cargo test
```

---

## 代码规范

### 通用

| 规则 | 说明 |
| --- | --- |
| `cargo fmt` | 提交前必须执行 |
| `cargo clippy` | 不得有 warning |
| **禁止 `unsafe`** | 所有 crate 已在 lib.rs 顶部声明 `#![forbid(unsafe_code)]` |
| **禁止 `unwrap()` / `expect()`** | 生产路径上一次都不行，已在 lib.rs 声明为 deny。测试代码例外 |
| 文档注释 | 所有 `pub` 项需有 `///`，crate 需有 `//!` |

### 加密与安全相关代码（额外要求）

这部分是本项目的风险集中区，审查会更严格：

| 要求 | 理由 |
| --- | --- |
| **不得自造密码学原语** | 只用成熟库的高层 API。手工拼装 AEAD 构造是本项目明确禁止的行为 |
| **密钥类型必须实现 `Zeroize` + `ZeroizeOnDrop`** | 密钥不得残留在内存 |
| **错误信息不得泄露可用于降低攻击成本的信息** | 例如解锁失败不区分"密码错"与"文件损坏" |
| **敏感数据不得进入日志** | 用 `LogSafe` trait 在编译期阻止，而不是靠代码审查 |
| **不得引入任何网络依赖** | 包括 `reqwest`、`hyper`、`ureq` 等。CI 会自动检查依赖树 |

### 依赖管理

新增依赖前请确认：

1. 它不在 `docs/04-系统设计.md` §11.1 的**禁止清单**中（网络、遥测相关一律禁止）
2. 其许可证与 MIT 兼容（**GPL / AGPL 一律禁止** —— 一旦链接，分发时整体将受 GPL 约束）
3. 它被加入 `core/Cargo.toml` 的 `[workspace.dependencies]`，并由各 crate 通过 `{ workspace = true }` 引用，而不是在各 crate 里散写版本号

---

## 提交规范

Commit message 用**祈使句**，说明"做了什么"而非"改了哪些文件"：

```
✓  修复导入 1PUX 时附件路径未做相对化处理
✓  为 lv-totp 补齐 RFC 6238 Appendix B 的 18 条测试向量
✗  update
✗  fix bug
✗  修改了 importer.rs
```

涉及设计变更的提交，请在 message 中引用对应文档章节（如 `docs/03-详细设计.md §6.3`）。

---

## 测试要求

| 场景 | 要求 |
| --- | --- |
| 一般功能 | 新增逻辑需有对应单元测试 |
| 密码学 | 必须通过标准测试向量（详见 `docs/04-系统设计.md` §8.2） |
| 导入器 | 必须补充针对畸形输入的测试 |
| Bug 修复 | 先写出能复现该 bug 的测试，再修复 |

### 测试数据的一条硬规则

**不得提交任何真实数据样本。**

- 需要测试数据时，用 `python3 tools/make_test_sample.py` 生成合成样本
- 若因校准需要接触真实样本，**必须在本机离线使用，不提交到仓库**，校准完成后删除
- 只把推导出的**映射表**（不含具体值）入库，并在映射表中记录校准所用样本的 SHA-256 指纹

理由：这个仓库是公开的，而密码库样本一旦泄露无法撤回。

---

## 安全漏洞

**请勿开公开 issue。** 按 [`SECURITY.md`](SECURITY.md) 的流程私下报告。

---

## 文档与代码同步

本项目有一条不可妥协的要求：**涉及加密格式的变更，必须同时更新文档。**

| 变更类型 | 需同步更新 |
| --- | --- |
| 功能范围 | `docs/01-需求分析.md` → `02` → `03` → `04`（连锁） |
| 架构或技术选型 | `docs/02-概要设计.md` → `03` |
| 加密参数或格式 | `docs/03-详细设计.md` §1~§3 → `docs/04-系统设计.md` §6 |

只改代码不改文档，是这类项目最常见的隐患来源（见 `docs/04-系统设计.md` §15.2）。审查者会据此打回 PR。

---

## 行为准则

参与本项目即表示同意遵守 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)。
