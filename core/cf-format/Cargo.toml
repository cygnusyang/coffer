[package]
name = "cf-format"
description = "负责库容器的结构层：头部读写、格式版本识别、格式迁移。"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
cf-crypto = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
ciborium = { workspace = true }
thiserror = { workspace = true }
zeroize = { workspace = true }

# MANIFEST.json 的完整性 MAC（HMAC-SHA256），见 docs/03-详细设计.md §1.5
hmac = { workspace = true }
sha2 = { workspace = true }

# [已确认] base64 0.22 —— header.json 内各 *_b64 字段的编解码。
#           Cargo.lock 已锁定 0.22.1（同时存在 0.23.1，属其他依赖的传递闭包，不影响）。
base64 = "0.22"

# 状态：header.json / 容器操作 / MANIFEST 已实现（M1-1）。
# 历史快照的 CBOR 序列化归 cf-store，不在本 crate。
