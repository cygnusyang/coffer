//! # cf-format —— 容器格式读写
//!
//! 负责库容器的结构层：头部读写、格式版本识别、格式迁移。
//!
//! ## 对应设计文档
//!
//! - `docs/03-详细设计.md` §1（容器格式规范）
//! - `docs/03-详细设计.md` §2（密钥派生——头部中的 KDF 参数部分）
//! - `docs/04-系统设计.md` §6（格式兼容与迁移策略）
//!
//! ## 职责边界
//!
//! 只处理容器结构，**不管理条目内容**（那是 `cf-store` 的职责），
//! 也**不做字段级加密**（那是 `cf-crypto` 与 `cf-store` 的组合职责）。
//!
//! 关键约束：格式变更必须先写进 `docs/03-详细设计.md` §1，再改代码。
//! 只改代码不改文档，是这类项目最常见的隐患来源
//! （见 `docs/04-系统设计.md` §15.2）。
//!
//! ## 状态
//!
//! **未实现** —— 计划在 **M1** 阶段实现。
//! 参见 `README.md`「当前状态」与 `docs/04-系统设计.md` §10.1（阶段划分）。
//!
//! 本文件当前只声明模块意图，不含任何可调用接口。
//! 这样做是刻意的：宁可目录里是空的，也不要一个 `todo!()` 占位的假接口。

#![forbid(unsafe_code)]
#![warn(missing_docs)]
