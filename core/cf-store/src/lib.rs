//! # cf-store —— 存储引擎
//!
//! SQLite schema、加密字段读写、事务、附件分块加密、历史版本。
//!
//! ## 对应设计文档
//!
//! - `docs/03-详细设计.md` §3（存储层设计）
//! - `docs/03-详细设计.md` §2.5（字段级 AEAD 与 AAD 构造）
//! - `docs/03-详细设计.md` §3.5（已知元数据泄露清单）
//!
//! ## 职责边界
//!
//! 负责「加密后的数据怎么落盘」，**不做字段级业务校验**（那是 `cf-domain` 的职责）。
//!
//! 关键约束：所有写入必须在一个事务里；
//! 附件必须分块加密，禁止整体载入内存
//! （见 `docs/01-需求分析.md` NFR-REL-01 / NFR-REL-04）。
//!
//! ## 状态
//!
//! **未实现** —— 计划在 **M1** 阶段实现。
//! 参见 `README.md`「当前状态」与 `docs/04-系统设计.md` §10.1（阶段划分）。
//!
//! 本文件当前只声明模块意图，不含任何可调用接口。
//! 这样做是刻意的：宁可目录里是空的，也不要一个 `todo!()` 占位的假接口。

#![forbid(unsafe_code)]
#![warn(missing_docs)]
