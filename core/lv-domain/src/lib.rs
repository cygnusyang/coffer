//! # lv-domain —— 领域模型
//!
//! 条目 / 字段 / 保险库的领域模型，22 类条目模板与校验规则。
//!
//! ## 对应设计文档
//!
//! - `docs/03-详细设计.md` §4（领域模型）
//! - `docs/03-详细设计.md` §4.1（条目类型）
//! - `docs/03-详细设计.md` §4.2（字段类型与 designation）
//!
//! ## 职责边界
//!
//! **纯逻辑，无 IO，无平台依赖**（见 `docs/02-概要设计.md` §2.3）。
//! 本 crate 不得依赖 `lv-store` / `lv-format` 等基础设施 crate，
//! 依赖方向必须单向：基础设施 → 领域 → 应用服务。
//!
//! ## 状态
//!
//! **未实现** —— 计划在 **M1** 阶段实现。
//! 参见 `README.md`「当前状态」与 `docs/04-系统设计.md` §10.1（阶段划分）。
//!
//! 本文件当前只声明模块意图，不含任何可调用接口。
//! 这样做是刻意的：宁可目录里是空的，也不要一个 `todo!()` 占位的假接口。

#![forbid(unsafe_code)]
#![warn(missing_docs)]
