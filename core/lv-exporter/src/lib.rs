//! # lv-exporter —— 导出器
//!
//! 加密备份包、1PUX 兼容格式、明文 CSV 的导出。
//!
//! ## 对应设计文档
//!
//! - `docs/01-需求分析.md` §5-H（FR-8 数据导出与备份）
//! - `docs/03-详细设计.md` §1.4（交换形态打包格式）
//!
//! ## 职责边界
//!
//! 关键约束：
//! 1. 明文导出（CSV）必须由调用方先完成二次确认（FR-8.4）；
//! 2. 导出 1PUX 时需注意 CSV 公式注入防护（详见 §6.4.4）；
//! 3. 打包前必须执行 `PRAGMA wal_checkpoint(TRUNCATE)` 合并 WAL。
//!
//! ## 状态
//!
//! **未实现** —— 计划在 **M2** 阶段实现。
//! 参见 `README.md`「当前状态」与 `docs/04-系统设计.md` §10.1（阶段划分）。
//!
//! 本文件当前只声明模块意图，不含任何可调用接口。
//! 这样做是刻意的：宁可目录里是空的，也不要一个 `todo!()` 占位的假接口。

#![forbid(unsafe_code)]
#![warn(missing_docs)]
