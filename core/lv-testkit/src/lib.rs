//! # lv-testkit —— 测试支持
//!
//! 测试夹具、合成样本生成、跨端一致性比对辅助。
//!
//! ## 对应设计文档
//!
//! - `docs/04-系统设计.md` §8（测试策略）
//! - `docs/04-系统设计.md` §8.4（测试数据管理）
//!
//! ## 职责边界
//!
//! 关键约束：**本 crate 不得包含任何真实数据样本**。
//! 合成样本由 `tools/make_test_sample.py` 生成，
//! 真实样本一律不入库（见 §8.4）。
//!
//! 注意：本 crate 是唯一允许使用 `unwrap()` / `expect()` 的
//! 生产侧 crate —— 但仅限于测试辅助路径，且需注释说明理由。
//!
//! ## 状态
//!
//! **未实现** —— 计划在 **M1** 阶段实现。
//! 参见 `README.md`「当前状态」与 `docs/04-系统设计.md` §10.1（阶段划分）。
//!
//! 本文件当前只声明模块意图，不含任何可调用接口。
//! 这样做是刻意的：宁可目录里是空的，也不要一个 `todo!()` 占位的假接口。

#![forbid(unsafe_code)]
#![warn(missing_docs)]
