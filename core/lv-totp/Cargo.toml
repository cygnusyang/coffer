[package]
name = "lv-totp"
description = "按 RFC 6238 生成 TOTP，解析 otpauth:// URI。"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
hmac = { workspace = true }
sha1 = { workspace = true }
sha2 = { workspace = true }
zeroize = { workspace = true }
thiserror = { workspace = true }

# 状态：未实现（计划 M1 阶段）
